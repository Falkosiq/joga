<!doctype html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7" lang=""> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8" lang=""> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9" lang=""> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js" lang=""> <!--<![endif]-->
<head>
    <meta charset="utf-8">
    <!--<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Joga Centrum Włocławek</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="apple-touch-icon" href="apple-touch-icon.png">
    <link rel="stylesheet" href="{{ asset('style/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('style/styles.css') }}">
    <link rel="stylesheet" href="{{ asset('style/queries.css') }}">
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="{{ asset('style/aos.css') }}">

</head>
<body>
<section class="hero" id="hero">
    <section class="navigation text-center">
        <header>
            <div class="header-content">
                <div class="logo"><a href="#hero"><img src="{{ asset('img/logo_mini.png') }}" alt="logo"></a></div>
                <div class="header-nav">
                    <nav>
                        <ul class="primary-nav">
                            <li><a href="#about">O Centrum</a></li>
                            <li><a href="#activities">Zajęcia</a></li>
                            <li><a href="#schedule">Plan zajęć</a></li>
                            <li><a href="#prices">Cennik</a></li>
                            <li><a href="#terms">Regulamin</a></li>
                            <li><a href="#workshops">Warsztaty</a></li>
                            <li><a href="#contact">Kontakt</a></li>
                        </ul>
                    </nav>
                </div>
                <div class="navicon">
                    <a class="nav-toggle" href="#"><span></span></a>
                </div>
            </div>
        </header>
    </section>
    <div class="container">
        <div class="row">
            <div class="col-md-10 col-md-offset-1">
                <div class="hero-content text-center">
                    <h3 style="font-weight:400;">Joga Centrum</h3>
                    <div class="logo"><img src="{{ asset('img/logo.png') }}" alt="logo" class="img-center img-responsive"></div>
                    <h3>Ewa Falkowska</h3>

                </div>
            </div>
        </div>
    </div>
    <div class="down-arrow floating-arrow"><a href="#about"><i class="fa fa-angle-down"></i></a></div>
</section>
{%  if sections|length >=6 %}
    {% set sec0 = sections.0 %}
    {% set sec1 = sections.1 %}
    {% set sec2 = sections.2 %}
    {% set sec3 = sections.3 %}
    {% set sec4 = sections.4 %}
    {% set sec5 = sections.5 %}
<section id="about" class="about">
    <div class="full-container about-wrap">
        <img class="about-img img-responsive" src="{{ asset('img/' ~ sec0.photo) }}">
        <br><p>{{ sec0.content|nl2br }}</p>
    </div>

</section>
<section class="activities" id="activities">
    <div class="full-container">
        <div class="row vcenter">
            <div data-aos="slide-right" class="col-md-6 act shaped text-center" style="background:url('{{ asset('img/' ~ sec1.photo) }}'); background-size:cover;">
                <h1>{{ sec1.title }}</h1>
            </div>
            <div data-aos="zoom-in" class="col-md-6 act-text">
                {{ sec1.content|nl2br }}
            </div>
        </div>
    </div>
</section>
<section class="schedule" id="schedule">
    <div class="full-container">
        <div class="row vcenter">
            <div data-aos="slide-right" class="col-md-6 act shaped-2 text-center">
                <h1>Plan Zajęć</h1>
            </div>
            <div data-aos="zoom-in" class="col-md-6 act-text">
                <div class="row">
                    <div class="sch-tab col-md-2">Pn.</div>
                    {%  for activity in activities %}
                        {% if activity.day == "1" %}
                    <div class="sch-cell col-lg-2 col-md-3">{{ activity.hour }}<p>{{ activity.type  }}</p></div>
                        {% endif %}
                    {% endfor %}

                </div>
                <div class="row">
                    <div class="sch-tab col-md-2">Wt.</div>
                    {%  for activity in activities %}
                        {% if activity.day == "2" %}
                            <div class="sch-cell col-lg-2 col-md-3">{{ activity.hour }}<p>{{ activity.type  }}</p></div>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="row">
                    <div class="sch-tab col-md-2">Śr.</div>
                    {%  for activity in activities %}
                        {% if activity.day == "3" %}
                            <div class="sch-cell col-lg-2 col-md-3">{{ activity.hour }}<p>{{ activity.type  }}</p></div>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="row">
                    <div class="sch-tab col-md-2">Czw.</div>
                    {%  for activity in activities %}
                        {% if activity.day == "4" %}
                            <div class="sch-cell col-lg-2 col-md-3">{{ activity.hour }}<p>{{ activity.type  }}</p></div>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="row">
                    <div class="sch-tab col-md-2">Pt.</div>
                    {%  for activity in activities %}
                        {% if activity.day == "5" %}
                            <div class="sch-cell col-lg-2 col-md-3">{{ activity.hour }}<p>{{ activity.type  }}</p></div>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</section>
<section class="prices" id="prices">
    <div class="full-container">
        <div class="row vcenter">
            <div data-aos="slide-right" class="col-md-6 act shaped text-center" style="background:url('{{ asset('img/' ~ sec2.photo) }}'); background-size:cover;">
                <h1>{{ sec2.title }}</h1>
            </div>
            <div data-aos="zoom-in" class="col-md-6 act-text">
                {{ sec2.content|nl2br }}
            </div>
        </div>
    </div>
</section>
    <section class="terms" id="terms">
        <div class="full-container">
            <div class="row vcenter">
                <div data-aos="slide-right" class="col-md-6 act shaped-2 text-center" style="background:url('{{ asset('img/' ~ sec3.photo) }}'); background-size:cover;">
                    <h1>{{ sec3.title }}</h1>
                </div>
                <div data-aos="zoom-in" class="col-md-6 act-text">
                    {{ sec3.content|nl2br }}
                </div>
            </div>
        </div>
    </section>
    <section class="workshops" id="workshops">
        <div class="full-container">
            <div class="row vcenter">
                <div data-aos="slide-right" class="col-md-6 act shaped text-center" style="background:url('{{ asset('img/' ~ sec4.photo) }}'); background-size:cover;">
                    <h1>{{ sec4.title }}</h1>
                </div>
                <div data-aos="zoom-in" class="col-md-6 act-text">
                    {{ sec4.content|nl2br }}
                </div>
            </div>
        </div>
    </section>
    <section class="contact" id="contact">
        <div class="full-container">
            <div class="row vcenter">
                <div data-aos="slide-right" class="col-md-6 act shaped-2 text-center" style="background:url('{{ asset('img/' ~ sec5.photo) }}'); background-size:cover;">
                    <h1>{{ sec5.title }}</h1>
                </div>
                <div data-aos="zoom-in" class="col-md-6 act-text">
                    {{ sec5.content|raw }}
                </div>
            </div>
        </div>
    </section>

{% endif %}

<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
<script>window.jQuery || document.write('<script src="js/vendor/jquery-1.11.2.min.js"><\/script>')</script>
<script>
    /* When user clicks the Icon */
    $('.nav-toggle').click(function() {
        $(this).toggleClass('active');
        $('.header-nav').toggleClass('open');
        event.preventDefault();
    });
    /* When user clicks a link */
    $('.header-nav li a').click(function() {
        $('.nav-toggle').toggleClass('active');
        $('.header-nav').toggleClass('open');

    });

    $(function() {
        $(window).scroll(function() {
            var scroll = $(window).scrollTop();

            if (scroll >= 20) {
                $('section.navigation').addClass('fixed');
                $('header').css({
                    "border-bottom": "none",
                    "padding": "12px 0"
                });
                $('header .member-actions').css({
                    "top": "26px",
                });
                $('header .navicon').css({
                    "top": "34px",
                });
            } else {
                $('section.navigation').removeClass('fixed');
                $('header').css({
                    "border-bottom": "solid 1px rgba(255, 255, 255, 0.2)",
                    "padding": "26px 0"
                });
                $('header .member-actions').css({
                    "top": "41px",
                });
                $('header .navicon').css({
                    "top": "48px",
                });
            }
        });
    });
    $(function() {

        $('a[href*=#]:not([href=#])').click(function() {
            if (location.pathname.replace(/^\//, '') === this.pathname.replace(/^\//, '') && location.hostname === this.hostname) {

                var target = $(this.hash);
                target = target.length ? target : $('[name=' + this.hash.slice(1) + ']');
                if (target.length) {
                    $('html,body').animate({
                        scrollTop: target.offset().top
                    }, 800);
                    return false;
                }
            }
        });

    });
</script>
<script src="{{ asset('js/aos.js') }}"></script>
<script>
    AOS.init({
        easing: 'ease-in-out-sine',
        duration: 1000,
    });
</script>
</body>
</html>